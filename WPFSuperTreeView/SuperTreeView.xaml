<UserControl x:Class="WPFSuperTreeView.SuperTreeView"
             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFSuperTreeView"
             xmlns:WPFCustomCommands="clr-namespace:WPFCustomCommands;assembly=WPFCustomCommands"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500" x:Name="superTree">

    <ScrollViewer x:Name="scrollViewer1"  Margin="0,0,0,0"
                      VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="PapayaWhip" >
        <DockPanel>
            <TreeView x:Name="tree" VerticalContentAlignment="Stretch"   SelectedItemChanged="InnerTreeSelectedItemChanged"   Background="PapayaWhip"  PreviewMouseDoubleClick="ClickToReName"   />
            <!--<TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}"
                           BasedOn="{StaticResource TreeViewItem1}">
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>-->
        </DockPanel>
    </ScrollViewer>





</UserControl>
